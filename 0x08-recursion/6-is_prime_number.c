#include "main.h"

/**
 * is_prime_number - returns 1 for prime numbers
 * @n: number
 *
 * Return 1 or 0;
 */
int is_prime_number(int n)
{
	if (n < 0 || n == 0 || n == 1)
	{
		return (0);
	}

	return (1);
}
