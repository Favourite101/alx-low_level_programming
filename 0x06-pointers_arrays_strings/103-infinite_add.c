#include "main.h"

/**
 * infinite_add - adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: buffer that will store the result
 * @size_r: size of r
 *
 * Return: r
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	r = n1 + n2;
	if (r > n - 1)
		return (0);

		return (r);
}
